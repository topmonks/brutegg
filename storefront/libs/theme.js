import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#314057",
    },
    secondary: {
      main: "#EA2122",
    },
  },
});

export default theme;
